<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Plain Material3-less DayNight theme over Material Components --
         this is a single-screen setup utility, not a designed product,
         so it intentionally doesn't carry a custom color/typography
         system the way the web dashboard's --mu-* tokens do. -->
    <style name="Theme.TopoTraceAgent" parent="Theme.MaterialComponents.DayNight.NoActionBar" />
</resources>
